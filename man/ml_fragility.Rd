% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_fragility.R
\name{ml_fragility}
\alias{ml_fragility}
\title{Leave-One-Out and Leave-k-Out Fragility Analysis}
\usage{
ml_fragility(
  data,
  meta_obj,
  yi,
  vi,
  study,
  time,
  max_k = 5L,
  max_combinations = 500L,
  alpha = NULL,
  rho = 0.8,
  small_sample = TRUE,
  seed = NULL
)
}
\arguments{
\item{data}{Long-format \code{data.frame}.}

\item{meta_obj}{Output from \code{\link[=ml_meta]{ml_meta()}}.}

\item{yi, vi, study, time}{Column names.}

\item{max_k}{Maximum number of studies to remove.  Default \code{5}.}

\item{max_combinations}{Maximum number of combinations to test per \eqn{k}.
Default \code{500}.  Larger values are more exhaustive but slower.}

\item{alpha}{Significance level.}

\item{rho}{Working correlation.}

\item{small_sample}{Use CR2 + Satterthwaite?}

\item{seed}{Random seed for sampling combinations.  Default \code{NULL}.}
}
\value{
Object of class \code{ml_fragility} (a \code{data.frame}) with columns:
\describe{
\item{\code{time}}{Follow-up time.}
\item{\code{k_studies}}{Number of studies at this time point.}
\item{\code{p_original}}{Original p-value.}
\item{\code{sig_original}}{Was the original result significant?}
\item{\code{fragility_index}}{Min number of removals to flip significance.
\code{NA} if not found within \code{max_k}.}
\item{\code{fragility_quotient}}{\code{fragility_index / k_studies} (proportion).}
\item{\code{study_removed}}{Study ID whose removal achieved the flip
(leave-one-out only).}
}
}
\description{
Computes fragility indices for each time point by systematically removing
studies and re-estimating the pooled effect.  The fragility index at time
\eqn{t} is the minimum number of studies whose removal changes the
statistical conclusion (significant -> non-significant or vice versa).
}
\details{
At each time point, studies are removed one at a time (or in combinations
for the leave-k-out version) and the model is re-fit.  The fragility index
is the smallest \eqn{k} such that removing any set of \eqn{k} studies
flips the significance of the pooled estimate.  A fragility index of 1
means a single study's removal changes the conclusion.

For the leave-k-out version, a random sample of combinations is used when
the number of combinations is large (controlled by \code{max_combinations}).
}
\examples{
\donttest{
dat  <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12), seed = 5)
meta <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time")
frag <- ml_fragility(dat, meta, yi = "yi", vi = "vi",
                      study = "study", time = "time",
                      max_k = 1L, seed = 1)
print(frag)
}

}
