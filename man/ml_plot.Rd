% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_plot.R
\name{ml_plot}
\alias{ml_plot}
\title{Combined Publication-Ready Trajectory Figure}
\usage{
ml_plot(
  meta_obj,
  sens_obj = NULL,
  bench_obj = NULL,
  spline_obj = NULL,
  frag_obj = NULL,
  ncol = NULL,
  main = NULL,
  col_effect = "#2166ac",
  col_sens = "#d73027",
  col_spline = "#1a9641",
  delta = NULL
)
}
\arguments{
\item{meta_obj}{Output from \code{\link[=ml_meta]{ml_meta()}} (required).}

\item{sens_obj}{Output from \code{\link[=ml_sens]{ml_sens()}} (optional; adds ITCV panel).}

\item{bench_obj}{Output from \code{\link[=ml_benchmark]{ml_benchmark()}} (optional; adds benchmark marks).}

\item{spline_obj}{Output from \code{\link[=ml_spline]{ml_spline()}} (optional; overlays spline).}

\item{frag_obj}{Output from \code{\link[=ml_fragility]{ml_fragility()}} (optional; adds fragility panel).}

\item{ncol}{Number of columns in the panel layout.  Default auto.}

\item{main}{Overall figure title.}

\item{col_effect}{Colour for the pooled effect trajectory.}

\item{col_sens}{Colour for the ITCV line.}

\item{col_spline}{Colour for the spline curve.}

\item{delta}{Fragility benchmark line on the ITCV panel.  Inherits from
\code{sens_obj} if available.}
}
\value{
Invisibly returns a list of the objects passed in.
}
\description{
Produces a multi-panel figure combining the pooled trajectory, confidence
band, spline fit (if supplied), and ITCV sensitivity profile.  Designed for
direct inclusion in manuscripts.
}
\examples{
dat  <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12), seed = 1)
meta <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time")
sens <- ml_sens(dat, meta, yi = "yi", vi = "vi", study = "study", time = "time")
ml_plot(meta, sens_obj = sens)

\donttest{
spl  <- ml_spline(meta, df = 2)
ml_plot(meta, sens_obj = sens, spline_obj = spl,
        main = "Longitudinal Meta-Analysis Profile")
}

}
