% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_meta.R
\name{ml_meta}
\alias{ml_meta}
\title{Longitudinal Meta-Analysis with Robust Variance Estimation}
\usage{
ml_meta(
  data,
  yi,
  vi,
  study,
  time,
  alpha = 0.05,
  rho = 0.8,
  small_sample = TRUE,
  min_k = 2L,
  method = "REML",
  engine = c("rma.uni", "rma.mv")
)
}
\arguments{
\item{data}{A \code{data.frame} in \strong{long format}: one row per study x time point.}

\item{yi}{Character. Name of the effect-size column.}

\item{vi}{Character. Name of the sampling-variance column.}

\item{study}{Character. Name of the study-ID column (cluster variable).}

\item{time}{Character. Name of the follow-up time column (numeric).}

\item{alpha}{Significance level for confidence intervals and p-values.
Default \code{0.05}.}

\item{rho}{Assumed within-study correlation between effect sizes (used only
when \code{engine = "rma.mv"}).  Default \code{0.8}.}

\item{small_sample}{Logical. If \code{TRUE} (default), applies CR2 sandwich
variance estimation with Satterthwaite degrees of freedom (Tipton, 2015).
If \code{FALSE}, uses uncorrected z-based inference.}

\item{min_k}{Integer. Minimum number of studies required to fit a model at
a given time point.  Default \code{2}.}

\item{method}{Character. Variance estimator passed to metafor.  Default
\code{"REML"}.}

\item{engine}{Character. Fitting engine: \code{"rma.uni"} (default) or
\code{"rma.mv"}.  See section \emph{Engine choice}.}
}
\value{
An object of class \code{ml_meta} (a \code{data.frame}) with one row per time
point and columns: \code{time}, \code{k}, \code{theta}, \code{se}, \code{df}, \code{t_stat}, \code{p_val},
\code{ci_lb}, \code{ci_ub}, \code{tau2}, \code{note}.

Attributes:
\describe{
\item{\code{"fits"}}{Named list of fitted model objects (one per time point).}
\item{\code{"weights_by_time"}}{Named list of weight vectors for downstream
use by \code{\link[=ml_sens]{ml_sens()}} and \code{\link[=ml_benchmark]{ml_benchmark()}}.}
\item{\code{"engine"}, \code{"alpha"}, \code{"rho"}, \code{"small_sample"}}{Call metadata.}
}
}
\description{
Fits a random-effects meta-analytic model at each unique time point in a
long-format dataset of multi-wave effect sizes.  Inference uses robust
variance estimation (RVE) with optional Tipton (2015) small-sample
corrections via the \code{clubSandwich} package.
}
\section{Engine choice}{

Two fitting engines are supported:
\describe{
\item{\code{"rma.uni"} (default)}{\code{metafor::rma.uni()} -- appropriate when each
study contributes exactly one effect size per time point.  Simpler,
faster, and stores \code{tau2} directly from the REML estimate.}
\item{\code{"rma.mv"}}{\code{metafor::rma.mv()} with a prebuilt working covariance
matrix -- appropriate when studies contribute \emph{multiple} effect sizes at
the same time point (dependent effects within cluster).  Requires the
\code{rho} argument.}
}
}

\examples{
dat <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12), seed = 1)
result <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time")
print(result)
plot(result)

# rma.mv engine for dependent effects
\donttest{
result_mv <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time",
                     engine = "rma.mv", rho = 0.8)
}

}
\references{
Hedges, L. V., Tipton, E., & Johnson, M. C. (2010). Robust variance
estimation in meta-regression with dependent effect size estimates.
\emph{Research Synthesis Methods}, 1(1), 39-65.

Tipton, E. (2015). Small sample adjustments for robust variance estimation
with meta-regression. \emph{Psychological Methods}, 20(3), 375-393.
}
\seealso{
\code{\link[=ml_sens]{ml_sens()}}, \code{\link[=ml_benchmark]{ml_benchmark()}}, \code{\link[=ml_spline]{ml_spline()}}
}
