% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_spline.R
\name{ml_spline}
\alias{ml_spline}
\title{Spline-Based Nonlinear Time Trend in Longitudinal Meta-Analysis}
\usage{
ml_spline(meta_obj, df = 3L, n_pred = 200L, alpha = NULL, test_linear = TRUE)
}
\arguments{
\item{meta_obj}{Output from \code{\link[=ml_meta]{ml_meta()}}.}

\item{df}{Degrees of freedom for the natural cubic spline.  Default
\code{3}.  A value of \code{1} recovers a linear fit.}

\item{n_pred}{Number of prediction points for the smooth curve.  Default
\code{200}.}

\item{alpha}{Confidence level (inherits from \code{meta_obj} if \code{NULL}).}

\item{test_linear}{Logical.  If \code{TRUE}, performs an F-test of nonlinearity
(spline df > 1 vs linear fit).  Default \code{TRUE}.}
}
\value{
Object of class \code{ml_spline} with elements:
\describe{
\item{\code{pred}}{\code{data.frame} with \code{time}, \code{fit}, \code{ci_lb}, \code{ci_ub} for
the smooth prediction grid.}
\item{\code{coef}}{Spline coefficient estimates.}
\item{\code{vcov}}{Coefficient covariance matrix.}
\item{\code{r_squared}}{Weighted R-squared of the spline fit.}
\item{\code{p_nonlinear}}{p-value for nonlinearity test (if requested).}
\item{\code{df}}{Spline degrees of freedom used.}
\item{\code{meta_obj}}{The original \code{ml_meta} object (for plotting).}
}
}
\description{
Fits a natural cubic spline meta-regression over follow-up time using the
pooled time-point estimates from \code{\link[=ml_meta]{ml_meta()}}.  Produces a smooth pooled
trajectory with simultaneous pointwise confidence bands and tests for
nonlinearity.
}
\details{
The spline is fit by weighted least squares on the \code{ml_meta()} estimates,
using \code{1 / se^2} as weights (i.e., inverse squared SE weighting to reflect
the precision of each time-point estimate).  This is a second-stage model.

For a fully joint spline model at the individual-effect level, users should
call \code{\link[metafor:rma.mv]{metafor::rma.mv()}} directly with \code{mods = ~ ns(time, df)}.  This
function is primarily intended for visualisation and trajectory testing.
}
\examples{
dat  <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12, 24), seed = 3)
meta <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time")
spl  <- ml_spline(meta, df = 2)
print(spl)
plot(spl)

}
\seealso{
\code{\link[=ml_meta]{ml_meta()}}, \code{\link[=ml_plot]{ml_plot()}}
}
