% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_sens.R
\name{ml_sens}
\alias{ml_sens}
\title{Time-Varying Sensitivity Analysis via Longitudinal ITCV}
\usage{
ml_sens(data, meta_obj, yi, vi, study, time, alpha = NULL, delta = 0.15)
}
\arguments{
\item{data}{A \code{data.frame} in long format (same as passed to \code{\link[=ml_meta]{ml_meta()}}).}

\item{meta_obj}{Output from \code{\link[=ml_meta]{ml_meta()}}.}

\item{yi, vi, study, time}{Column names (same meaning as in \code{\link[=ml_meta]{ml_meta()}}).}

\item{alpha}{Significance level.  Defaults to the value stored in
\code{meta_obj} (or \code{0.05} if absent).}

\item{delta}{Numeric. User-defined practical fragility benchmark: time
points with \code{ITCV_alpha(t) < delta} are flagged as "practically fragile".
Default \code{0.15}.}
}
\value{
An object of class \code{ml_sens} (a \code{data.frame}) with columns:
\describe{
\item{\code{time}}{Follow-up time.}
\item{\code{theta}, \code{se}, \code{df}}{Copied from \code{meta_obj}.}
\item{\code{sy}}{Weighted SD of observed effect sizes.}
\item{\code{r_effect}}{Pooled effect on correlation scale.}
\item{\code{itcv}}{Raw ITCV: confounding needed to nullify the estimate.}
\item{\code{itcv_alpha}}{Significance-adjusted ITCV: confounding needed to
make the result non-significant.}
\item{\code{fragile}}{Logical; \code{TRUE} when \code{itcv_alpha < delta}.}
}
Attributes include trajectory summaries (\code{itcv_min}, \code{itcv_mean},
\code{fragile_prop}) and a \code{"fragile_times"} character vector.
}
\description{
Computes the Impact Threshold for a Confounding Variable (ITCV) at each
follow-up time point using the pooled estimates and robust inference from
\code{\link[=ml_meta]{ml_meta()}}.  Two versions are returned: the raw ITCV (threshold to nullify
the pooled effect) and the significance-adjusted ITCV_alpha (threshold to render
the result non-significant under small-sample-corrected inference).
}
\section{Mathematical background}{

At each time \eqn{t}, let \eqn{\hat\theta_t} be the pooled effect,
\eqn{s_{y,t}^2} the weighted variance of observed effect sizes, and
\eqn{c_t = t_{1-\alpha/2,\nu_t} \cdot \widehat{SE}(\hat\theta_t)} the
minimum effect still deemed significant.  The correlation-scale pooled effect
is
\deqn{r_t = \hat\theta_t / \sqrt{\hat\theta_t^2 + s_{y,t}^2}}
and the raw ITCV is \eqn{\sqrt{|r_t|}}.  The significance-adjusted version
replaces \eqn{\hat\theta_t} with \eqn{|\hat\theta_t| - c_t}.
}

\examples{
dat  <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12), seed = 1)
meta <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time")
sens <- ml_sens(dat, meta, yi = "yi", vi = "vi", study = "study", time = "time")
print(sens)
plot(sens)

}
\references{
Frank, K. A. (2000). Impact of a confounding variable on a regression
coefficient. \emph{Sociological Methods & Research}, 29(2), 147-194.
}
\seealso{
\code{\link[=ml_meta]{ml_meta()}}, \code{\link[=ml_benchmark]{ml_benchmark()}}, \code{\link[=ml_plot]{ml_plot()}}
}
