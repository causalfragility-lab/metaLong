% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_longitudinal_meta.R
\name{sim_longitudinal_meta}
\alias{sim_longitudinal_meta}
\title{Simulate a Longitudinal Meta-Analytic Dataset}
\usage{
sim_longitudinal_meta(
  k = 20L,
  times = c(0, 6, 12, 24),
  mu = 0.4,
  tau = 0.2,
  v_range = c(0.02, 0.12),
  missing_prop = 0,
  add_covariates = TRUE,
  seed = NULL
)
}
\arguments{
\item{k}{Number of studies.  Default \code{20}.}

\item{times}{Numeric vector of follow-up time points.
Default \code{c(0, 6, 12, 24)}.}

\item{mu}{Named numeric vector of true effects at each time point,
or a single value (recycled).  Default \code{0.4}.}

\item{tau}{Between-study SD.  Default \code{0.2}.}

\item{v_range}{Two-element vector for the uniform sampling variance range.
Default \code{c(0.02, 0.12)}.}

\item{missing_prop}{Proportion of study x time combinations to set missing
(simulates unbalanced follow-up).  Default \code{0.0}.}

\item{add_covariates}{Logical.  If \code{TRUE}, adds study-level covariates
\code{pub_year}, \code{quality}, and \code{n} for use with \code{\link[=ml_benchmark]{ml_benchmark()}}.
Default \code{TRUE}.}

\item{seed}{Random seed.  Default \code{NULL}.}
}
\value{
A \code{data.frame} in long format with columns:
\describe{
\item{\code{study}}{Study identifier (character).}
\item{\code{time}}{Follow-up time.}
\item{\code{yi}}{Observed effect size.}
\item{\code{vi}}{Sampling variance.}
\item{\code{pub_year}, \code{quality}, \code{n}}{Study-level covariates
(if \code{add_covariates = TRUE}).}
}
}
\description{
Generates a synthetic long-format dataset suitable for testing and
illustrating all \code{metaLong} functions.  Studies contribute effect sizes at
multiple follow-up time points with within-study correlation.
}
\details{
The true effect at time \eqn{t} for study \eqn{i} is
\deqn{\theta_{it} = \mu_t + u_i + \epsilon_{it}}
where \eqn{\mu_t} is a time-varying mean effect (optionally nonlinear),
\eqn{u_i \sim N(0, \tau^2)} is a study-level random effect, and
\eqn{\epsilon_{it} \sim N(0, v_{it})} is sampling error.  Within-study
correlation between time points is introduced through \eqn{u_i}.
}
\examples{
dat <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12), seed = 42)
head(dat)

# Nonlinear true trajectory
\donttest{
mu_t <- c("0" = 0.2, "6" = 0.5, "12" = 0.4, "24" = 0.1)
dat2 <- sim_longitudinal_meta(k = 10, times = c(0, 6, 12, 24), mu = mu_t,
                               missing_prop = 0.1, seed = 99)
}

}
