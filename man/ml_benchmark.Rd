% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_benchmark.R
\name{ml_benchmark}
\alias{ml_benchmark}
\title{Benchmark Calibration of Longitudinal ITCV Against Observed Covariates}
\usage{
ml_benchmark(
  data,
  meta_obj,
  sens_obj,
  yi,
  vi,
  study,
  time,
  covariates,
  alpha = NULL,
  rho = 0.8,
  small_sample = TRUE,
  min_k = 3L
)
}
\arguments{
\item{data}{Long-format \code{data.frame}.}

\item{meta_obj}{Output from \code{\link[=ml_meta]{ml_meta()}}.}

\item{sens_obj}{Output from \code{\link[=ml_sens]{ml_sens()}}.}

\item{yi, vi, study, time}{Column names.}

\item{covariates}{Character vector of observed moderator column names to benchmark.}

\item{alpha}{Significance level (inherits from \code{meta_obj} if \code{NULL}).}

\item{rho}{Working within-study correlation for V matrix.}

\item{small_sample}{Logical; use CR2 + Satterthwaite?}

\item{min_k}{Minimum studies required at a time point.  Default \code{3L}
(one extra relative to \code{\link[=ml_meta]{ml_meta()}} because regression needs more d.f.).}
}
\value{
Object of class \code{ml_benchmark} (a \code{data.frame}) with columns:
\describe{
\item{\code{time}}{Follow-up time.}
\item{\code{covariate}}{Covariate name.}
\item{\code{k}}{Number of studies.}
\item{\code{r_partial}}{Partial correlation of covariate with effect size.}
\item{\code{t_stat}, \code{df}, \code{p_val}}{RVE inference for the covariate slope.}
\item{\code{itcv_alpha}}{ITCV_alpha threshold at this time point.}
\item{\code{beats_threshold}}{Logical: does \verb{|r_partial| >= itcv_alpha}?}
\item{\code{skip_reason}}{Character; reason a cell was skipped, else \code{NA}.}
}
The \code{"fragile_summary"} attribute contains one row per time with counts.
}
\description{
For each follow-up time point, regresses each observed study-level covariate
on the effect sizes using RVE meta-regression, extracts the covariate's
partial correlation with the outcome, and compares it to the
significance-adjusted ITCV threshold from \code{\link[=ml_sens]{ml_sens()}}.  A covariate that
\emph{beats} the threshold demonstrates that real-world confounding of at least
that magnitude exists, which is direct evidence of effect fragility.
}
\section{Interpretation}{

If an observed covariate (e.g., publication year, sample quality, attrition
rate) has \verb{|r_partial| >= ITCV_alpha(t)}, then an \emph{unobserved} confounder with
the same relationship to exposure and outcome would be sufficient to nullify
the pooled effect at time \eqn{t}.  This does not prove confounding--it
calibrates the plausibility threshold.
}

\examples{
\donttest{
dat   <- sim_longitudinal_meta(k = 15, times = c(0, 6, 12), seed = 2)
meta  <- ml_meta(dat, yi = "yi", vi = "vi", study = "study", time = "time")
sens  <- ml_sens(dat, meta, yi = "yi", vi = "vi", study = "study", time = "time")
bench <- ml_benchmark(dat, meta, sens,
                       yi = "yi", vi = "vi", study = "study", time = "time",
                       covariates = c("pub_year", "quality"))
print(bench)
plot(bench)
}

}
\seealso{
\code{\link[=ml_sens]{ml_sens()}}, \code{\link[=ml_meta]{ml_meta()}}
}
